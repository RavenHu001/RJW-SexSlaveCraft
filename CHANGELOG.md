# Changelog — SexSlaveCraft (TieJin Modify)

本文件用于记录本模组各次修改更新的**概述**。详细的技术说明、涉及文件与验证方法见 `Docs/` 目录下对应的详细记录文档。

---

## 2026-08-14 — 性奴特化系统修复

**修复**

- 修复调教面板切换性奴特化时删除全部特化 hediff、清零进度的问题：
  - 特化进度改为**按类型独立保存**，切换不再丢进度；
  - **终极化 hediff 永不被删除**（含切到「无」）；
  - 终极化方向在面板中**不可再选**，标注「（已终极化）」；
  - 奶牛储奶仓充能在切换特化时暂存、切回恢复。
- 修复公交车进度双轨（性交走 progress / 交易与调教走 hediff 严重度）互相覆盖的问题：统一为 progress 单轨，hediff 严重度双向同步；旧档积累的严重度自动并入进度。
- 修复凝胶注入的 hediff 无法继续训练的问题：新增 `ReconcileSpecialization` 对账逻辑（自动认领 hediff 对应特化、同步状态），每 250 tick 与凝胶注入时执行；旧存档死锁状态进游戏后自动自愈。

**功能 / UI**

- 宠物猫、宠物兔特化在面板中标注「（未完成）」（该两方向尚未实装经验来源，仅标记）。

**兼容性**

- 新增存档字段均有默认值，旧存档无缝兼容。

**构建**

- `Assemblies/Sexslavecraft.dll` 已更新。

详细记录：`Docs/性奴特化修复与更新记录.md`
