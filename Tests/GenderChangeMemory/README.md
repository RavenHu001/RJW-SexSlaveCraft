# 性别重置手术记忆回归测试

直接链接生产 `Recipe_GenderChange.cs`，读取实际手术 XML 与繁中、英文、俄文翻译，验证重复定义和术后记忆空值问题。

在仓库根目录运行：

```powershell
dotnet run --project Tests/GenderChangeMemory/GenderChangeMemory.csproj --configuration Release -- .
```

项目无包依赖，使用 `Tests/NuGet.Config` 的离线还原配置。末尾参数为待验证仓库根目录。

## 修复内容

- 删除第二个 `defName`，统一使用代码及译文已有的 `SSC_Thought_GenderChangeSuccess`。
- 保留记忆原有的 15 天持续时间、基础心情 −10 和一层堆叠限制。
- 添加记忆前检查定义不为空；缺失时保留 `GetNamed` 的错误诊断，跳过添加，避免手术末尾空引用。
- 新增测试方法、回调与本轮涉及的生产方法均有中文注释。

## 自动化结果与边界

同一组用例在修复前为 4/11 通过，修复后为 11/11 通过；使用本机 RimWorld、RJW、Harmony 及动画依赖的 Release 构建通过。

11 项覆盖定义只有一个名称、正常手术添加正确记忆、缺失定义安全收尾并保留诊断、失败手术、非男性目标、无心情需求、无需求跟踪器、重复调用，以及三种译文与定义的对应关系。

宿主根据已核对的本机 RimWorld 1.6 行为，按 XML 顺序赋值名称、在查询失败时返回空值，并在记忆入口访问 `IsMemory`，使原来的空引用可以复现。实际游戏的 XML 加载器本身未在宿主中运行；器官调整、外观刷新和手术失败判断仅作有限模拟，不验证 HAR、真实身体状态或完整 Unity 手术调度。

维护者已确认原问题存在，并在修复后确认游戏内修复成功，授权提交并上传。确认未细分各语言、场景和模组组合，不扩大为全部兼容路径均已实测。修复不会自动补发过去已经漏掉的术后记忆。

## 后续游戏内回归清单

1. 重启游戏加载更新后的 XML 和 DLL，对有心情需求的男性角色成功施行性别重置手术。
2. 确认身体调整完成，心情栏出现相应术后记忆，且不再出现定义找不到后紧接空引用的收尾错误。
3. 使用繁中、英文或俄文时，确认该记忆显示对应译文。
4. 确认手术失败时不添加成功记忆，无心情需求的角色仍能正常完成身体调整。
