# 调教员身份回归

使用 .NET 9，无第三方依赖，从仓库根目录运行：

```powershell
dotnet run --project Tests/TrainerIdentity/TrainerIdentity.csproj --configuration Release `
  -p:RestoreConfigFile="完整仓库路径/Tests/NuGet.Config" -- .
```

直接编译生产身份工具、开关存档字段、一次性迁移、指定调教员服务、绑定服务、日常 WorkGiver 和两个仪式角色。当前 53 项覆盖（含阶段 3B 的新规则、日常筛选与双向选角）：

- 主人/未选择固定状态，性奴可选及真实身份切换。
- 已绑定性奴和仍有绑定性奴的主人拒绝身份切换，保留锁链实例、严重度、恶堕与训练/仪式状态。
- 重复设置当前身份和重复绑定幂等；无绑定角色可切换，明确解绑后恢复资格。
- 绑定入口提前校验身份，拒绝把有绑定性奴的主人转成性奴，不残留半条绑定；首次绑定与归属冲突回归。
- 菜单和直接指派一致过滤，排除自身及原版候选范围外人员，工作条件和主人限制。
- 原版分类 getter 清空重填同一临时列表时，嵌套指派检查与外部分类查询不会破坏菜单候选枚举。
- 重新指派、停用记录保留、开放调教及公交车的身份门槛。
- 自动/强制工作、冷却排班和预留限制，停用后不得开始下一次调教。
- 绑定关系、无主归属回退、仪式角色与停用指派；绑定入口不再依据旧例外覆盖第三方或空指派。新规则的指派协调由 RestrictionCore 单独验证，本套件不安装生命周期 Harmony。
- 旧档缺字段、显式关闭、世界对象迁移及重复加载幂等。
- 四语提示键与格式参数。

不运行 Unity。存档使用字典模型，不能代替真实 Scribe 引用恢复；工作用例执行真实 WorkGiver，日常 JobDriver 的准备、结束回调由 RitualLifecycle 链接生产驱动验证，完整队列/走位仍需游戏内复测。总栏字体、缩放和鼠标操作也需游戏内确认。同床通过独立的 [SharedBed](../SharedBed/README.md) 套件验证。

本套件已纳入 `Scripts/Test-All.ps1` 和打包流程，统一入口会自动传入仓库绝对路径；上面的命令仍可用于单独排查。统一运行方式见 [发布与打包](../../Docs/Maintenance/发布与打包.md)。

2026-09-16 身份锁定修复验证：30/30 通过，Release 编译成功；维护者随后确认实机修复有效，授权更新文档并提交。该实机反馈不改变上述测试模型的覆盖边界。

原先模型把 `FreeColonists` 写为普通列表字段，未覆盖分类 getter 重建共享结果的行为。修正模型并加入停用调教员菜单场景后，旧实现 19/23 通过且四项抛出集合修改异常；生产枚举改为快照后 23/23 通过。分类成员由用例提供，模型不声称复现完整原版阵营及 DLC 条件。

阶段 3B 将旧的空指派和开放例外预期调整为唯一指定者规则，保留历史身份及迁移覆盖；新增主人手动发起、非主人独立调教条目、空/停用指派主持、两种选角顺序、首次准备及绑定不变等 10 项。

绑定门槛与指派整理新增 12 项：只读候选、HasJob 不分配临时任务、正式创建重查、残留恢复；指派同步授权、强制覆盖原子拒绝、无效候选不写入、未绑定不初始化、总开关关闭（含缺失/坏配置）；主人许可与工作准入分离；恢复期间解绑的原主/第三方指派清理。直接绑定通知以边界替身表示，实际配置生命周期由 RestrictionCore 验证。
