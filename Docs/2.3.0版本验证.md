# 2.3.0 版本归并与构建验证

记录日期：2026-09-16。玩法实现基线为 `a1b63c9`，版本归并提交 `0031caf` 经 PR #10 合入主分支；Baphomet 翻译贡献经 PR #11 合入，正式发布基于合并提交 `3011e95`。此前各项玩法改动已由维护者分阶段确认实机有效；本次仅更新发布记录并重新构建，不新增玩法逻辑。

## 版本范围

| 提交 | 内容 |
| --- | --- |
| `4c6582a` | 收窄同床额外许可、改为睡后记忆并处理定义不匹配 |
| `b1bb8e8` | Dubs Mint Menus 床位分配标记兼容 |
| `6fb8ae5` | 补齐同床与辅助函数注释 |
| `94b14e6` | 主人/指定调教员关系并集与空殖民者床分配修复 |
| `581dc72` | 统一调教员身份、旧档迁移及指定菜单红字修复 |
| `58273d3` | 仪式指定调教员不匹配的准确说明 |
| `ce1dc49` | 独立身份标签、SSC 许可状态与恶堕门槛提示 |
| `a1b63c9` | 锁定已绑定角色身份以保留锁链和成长进度 |
| `5f8c448`、`b1c82de` | 吸收 Baphomet 基于 2.2.15 的 38 条翻译并注明贡献来源 |

保留 2.2.15 及更早版本修复。旧 RimTalk、未完成猫/兔入口维持暂停状态，新个体限制系统等未来方案不计入 2.3.0 已实现内容。

## 版本与构建

| 项目 | 值 |
| --- | --- |
| `VERSION`、About 与四份中英文指南 | 2.3.0 |
| DLL 文件版本 | 2.3.0.0 |
| DLL 产品版本 | 2.3.0 |
| 程序集引用身份 | 1.0.0.0 |
| RimWorld 支持版本 | 1.6 |

使用本机 Visual Studio MSBuild、.NET Framework 4.7.2 和游戏/RJW/Harmony/动画框架依赖执行 Release Rebuild，退出码 0，无警告或错误。本机引用由已忽略的 `.builds/ritual-fix/LocalReferences.targets` 提供，命令：

```powershell
& 'D:\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe' `
  Sexslavecraft/SexSlaveCraft_Alpha.csproj /t:Rebuild /p:Configuration=Release `
  /p:CustomAfterMicrosoftCommonTargets=E:\Games\RimWorld\modDevelopment\RJW-SexSlaveCraft-TieJin-Modify\.builds\ritual-fix\LocalReferences.targets `
  /nologo /verbosity:minimal
```

对比 `a1b63c9` 提交中的 DLL，**1,692 个方法体、局部变量、异常处理区域及程序集依赖列表均一致**。版本归并只同步版本元数据。正式发布重编译时，产物哈希与归并 DLL 不同；再次核对全部 1,692 个方法体及依赖列表仍完全一致，使用本次重编译产物随最终发布提交交付，不引入额外玩法变更。

正式发布的 `Assemblies/Sexslavecraft.dll` 的 SHA-256：

```text
795CBA2254149D2CE9D0B99675D27C29DA4C9D32E2672C7F995DFAB1D83F8F2D
```

已核对 About XML、程序集源码与实际 DLL 的版本、当前指南版本、CHANGELOG 标题、新文档本地链接及打包脚本语法。历史 2.2.15 发布内容与下载链接保留，不改写为尚不存在的 2.3.0 Release 链接。

## 自动化回归

本轮运行 15 个套件，共 **390/390 通过**：

| 套件 | 通过数 |
| --- | ---: |
| RitualProgression | 29 |
| RitualLifecycle | 40 |
| PersonalityTraits | 23 |
| PersonalityInsertion | 16 |
| GenderChangeMemory | 11 |
| RitualAnimationFallback | 6 |
| PermanentLactation | 22 |
| PersonalityMemories | 16 |
| PersonalitySpecializations | 21 |
| PersonalityCardLayout | 18 |
| RaceInjection | 8 |
| InteractionProtection | 27 |
| BusTrade | 30 |
| TrainerIdentity | 30 |
| SharedBed | 93 |
| 合计 | 390 |

版本归并时，前 13 个套件按 `Scripts/New-Release.ps1` 的配置单独运行。正式发布时从最终提交的干净工作区执行该脚本的 `-Build` 流程，使用离线 `Tests/NuGet.Config` 重跑这 13 个套件并生成安装包；身份与同床两个额外套件在发布前独立运行。

`TrainerIdentity` 与 `SharedBed` 单独运行，尚未加入默认打包列表。前者直接编译生产身份、指派、绑定、迁移、工作与仪式代码；后者使用适用于 .NET 9 的 Harmony 2.4.2 执行床位、睡眠记忆与原版/Mint 行模型补丁。SharedBed 不能改用游戏的 .NET Framework Harmony DLL。命令见各自 [身份测试说明](../Tests/TrainerIdentity/README.md) 与 [同床测试说明](../Tests/SharedBed/README.md)。

## 实际程序集补丁检查

在独立 .NET Framework 进程对最终 2.3.0 DLL 执行两组检查，不调用完整游戏方法：

- 未加载 Mint：安装原版十处补丁，Mint 可选补丁数量为 0。
- 加载本机 Mint 1.6：原版补丁正常，Mint 行补丁数量为 1，姓名绘制替换两处。
- 实际 IL 中原版分配身份判断替换两处、已分配/未分配姓名各一处；没有新增全局恋爱判断补丁。

## 实机确认与边界

维护者已确认同床与记忆、Mint 显示、身份开关、指定菜单修复、仪式提示、独立标签及绑定身份保护有效。本次版本归并不声称重新完成所有模组组合的实机测试。

自动化模型不运行 Unity，不代替真实 Scribe 存档往返、游戏字体缩放或第三方补丁顺序检查。旧档迁移、停用指定对象及已丢失锁链不自动恢复的行为，已写入 [2.3.0 发布说明](2.3.0发布说明.md)。

## 正式发布验证

- 基于 `3011e95` 更新发布记录，提交后从干净工作区执行 `Scripts/New-Release.ps1 -Build -ReferenceTargetsPath .builds/ritual-fix/LocalReferences.targets -OutputDirectory Releases/Published/2.3.0`，重新编译并运行默认 13 套件；另外运行身份 30 项、同床 93 项，共 390/390 通过。
- 重新核对 Baphomet 导入的三个语言文件，共 38 条：XML、同类型重复键、实际定义字段、非空文本与占位符均通过。翻译本身未另做游戏内显示验证，不将静态检查等同于实机确认。
- 安装 ZIP 仅含模组运行目录、唯一的本模组 DLL 和文档；逐文件清单核对大小与 SHA-256，记录最终发布提交，`workingTreeDirty: false`、`assemblyRebuilt: true`。ZIP 配套 `.sha256` 文件。
- 发布提交推送到 `main`，`v2.3.0` 指向同一提交；先准备草稿并上传安装包和校验文件，核对服务端附件大小及 SHA-256，再设为最新正式版本。
- 产物：`Releases/Published/2.3.0/RJW-SexSlaveCraft-TieJin-Modify-2.3.0.zip` 及相邻校验文件。下载见 [v2.3.0 Release](https://github.com/RavenHu001/RJW-SexSlaveCraft/releases/tag/v2.3.0)，流程见 [发布与打包](发布与打包.md)。
