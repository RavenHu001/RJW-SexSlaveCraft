# 2.3.1 版本验证

日期：2026-09-22。运行版本：2.3.1；DLL 文件版本 2.3.1.0、产品版本 2.3.1，程序集身份保留 1.0.0.0。发布入口：[v2.3.1](https://github.com/RavenHu001/RJW-SexSlaveCraft/releases/tag/v2.3.1)。

## 本次构建与回归

- 使用本机 RimWorld/RJW 引用执行 Release 重建，版本元数据与 VERSION、About 一致。
- 发布预检执行 `Scripts/New-Release.ps1 -Build`；全部 16 套 **645/645** 通过，无失败或未运行套件。
- 核心相关套件：RestrictionCore 124、InteractionProtection 112、TrainerIdentity 53、RitualLifecycle 50、BusTrade 43、SharedBed 93；其余套件见本地 JSON 报告。
- 预检报告：`.builds/release-2.3.1/preflight-validation/summary.json`，构建日志：同目录上级的 `preflight-build.log`。正式包从提交后的干净工作区通过打包脚本生成，打包再次运行其要求的全量验证，报告位于 `.builds/release-2.3.1/published-validation/`。
- 本次运行代码只调整程序集版本元数据；玩法逻辑沿用已验收的新限制实现。同时更新中英文玩家指南中旧例外、旧装备独立保护、LifeForce 白名单等过时说明。

## 实机验收依据

维护者已确认旧档升级后再次存读档、走位与已开始场景中的规则变化、任务中途存读档、常用兼容组合及最新界面没有问题。具体范围见[开发完成与实机验收](../../Development/新限制系统开发完成与实机验收.md)。这些反馈沿用原验收，不标为本次重新实测。

## 发布包与追溯

- 产物：`Releases/Published/2.3.1/RJW-SexSlaveCraft-TieJin-Modify-2.3.1.zip` 及同名 `.zip.sha256`。
- 包内 `RELEASE-MANIFEST.json` 记录发布基础提交、工作区状态、每个文件的大小与 SHA-256 及打包时的验证结果。
- 安装包校验包括单一模组根目录、全部清单文件哈希、版本及 DLL 与提交内容一致性，并排除源码、测试、缓存和第三方 DLL。
- GitHub 上传先使用草稿；校验服务端附件大小及 SHA-256 与本机一致后公开发布，并核对正式标签对应提交及 Latest 状态。实际远端结果保存在 `.builds/release-2.3.1/github-release-final.json`；ZIP 哈希以随包校验文件为准，避免把自身哈希写进安装包形成循环。
- [Discord 中英公告](Discord公告.md)供维护者发布，不自动发送到频道。
