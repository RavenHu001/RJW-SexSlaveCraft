# 主奴同床优化

日期：2026-09-16。状态：此前同床、调教员身份及相关修复已提交。独立身份标签与 SSC 许可状态显示已通过自动检查和维护者实机确认，相关同床更新已随 [2.3.0](https://github.com/RavenHu001/RJW-SexSlaveCraft/releases/tag/v2.3.0) 正式发布。

## 已确认的范围

1. SSC 性奴的绑定主人和当前指定调教员同时是有效床伴，同一人去重；对象失效不会取消另一个有效对象。沿用当前调教员指派规则，不额外放宽调教权限。
2. 普通原版奴隶不会仅因奴隶身份获得额外床位身份许可、性奴标签或睡后心情。仅有恶堕或特殊关系不等于获得性奴身份。
3. 移除全局恋爱判定补丁。原版恋人、配偶同床照常，不禁止性奴与原版配偶同床。
4. 医疗和死眠优先；普通休息依次尝试自己的已分配床、主人床和调教员床，每张床都经过原版完整检查，全部不可用时保留原版找床结果。
5. 原版与 Mint 分配列表共用独立标签：主人（金色）、性奴（紫粉色）、调教员（青蓝色）。空床显示候选者的全部实际身份；有分配人员时只标记已分配者及其直接 SSC 床伴，并显示这些人的全部身份。多人取直接关系并集，不递归扩展。标签位于姓名区域右侧，不改变姓名起点。
6. 保留六档数值，将同床效果改为实际共同睡眠结束后的记忆，持续 1 天，不叠加。主人与调教员使用不同文案。

保留当前恶堕严格高于 0.1% 的门槛。原版奴隶和自由殖民者中的 SSC 性奴均可使用这项额外许可；囚犯仍遵守原版限制。

调教员身份调整已通过游戏内复测并提交：主人固定具备资格、未选择固定不具备、性奴按个人开关。调教员同床分支仅接受有效指定对象，原有主人、软限制及标签过滤规则保留，详见 [调教员身份开关](调教员身份开关.md)。

本轮不增加轮换、陪睡名单、同步作息或共同睡眠时长系统。只在双方确实同时睡着后记录本次共同睡眠，清醒躺卧和仅有同床分配不算。

## 实现

- `SSCSharedBedUtility` 只解析身份/对象及额外许可。删除全地图普通床回退扫描和复制的环境检查。
- `CanUseBedNow` 前缀仅为合格对象的普通床调整本次奴隶床分类参数；不修改 Pawn 的实际身份，继续执行完整原版方法。
- `BedOwnerWillShare` 仅补充床位共享许可；找床调用原版 `RestUtility.IsValidBedFor`，环境、体型、可达、禁用和预留检查由原版处理。
- `CanAssignTo` 仅替换原版方法中的奴隶身份分类查询，保留原版其余分配逻辑和拒绝理由。
- 候选展示独立于实际分配资格，普通多人床补充地图上的 SSC 性奴。原版奴隶身份的性奴不能直接分配空殖民者床，须先分配主人或指定调教员，再加入符合恶堕门槛的性奴。保留已有床位归属的身份例外；殖民者身份的性奴按原版规则分配。
- 界面标记只接入床位分配窗口的姓名绘制，不修改全局名字，也不影响其他建筑的分配界面。
- 当前仍是软限制：没有标签不隐藏角色行、不额外拒绝原版合法分配，也不解除已有归属。本轮不添加硬限制设置。
- `SSCSharedSleepRecord` 随 Pawn 训练组件存档。在实际共同睡眠时记录对象与阶段；结束躺卧时生成一次记忆并清空记录。同类主人/调教员记忆互相替换，不因多次休息或换对象叠加。
- 原有双方负面卧室/营房记忆免除保留，改用已经发生的共同睡眠记录，避免另一方提前起床导致记录丢失。主人/调教员不新增一份同床心情奖励。
- 主人和调教员都实际同睡过时仅保留主人记忆；主人先醒后不被调教员记录覆盖。两位实际同睡对象均保留负面房间记忆免除，无需新增存档字段。

| 条件（从上往下匹配） | 心情 |
| --- | ---: |
| 与本次对象有 `SSC_FlawedLovers` | +13 |
| 恶堕 ≥ 50% | +9 |
| 恶堕 ≥ 30% | +5 |
| 其余且好感 < 30 | -6 |
| 其余且好感 < 50 | -3 |
| 其余且好感 ≥ 50 | +2 |

`SSC_FlawedLovers` 仍可决定记忆的最高档，但不再因此被当作原版恋人，也不能越过性奴身份与当前对象条件。

## 正式发布验证

2.3.0 发布时 SharedBed 93/93、TrainerIdentity 30/30 通过，连同默认套件共 390 项通过；原版/Mint 真实程序集补丁检查通过。发布提交为 `29fcfd9`，完整产物与校验记录见 [2.3.0 版本验证](../Releases/2.3.0/2.3.0版本验证.md)。

## 关系与空床修复阶段验证记录

- 使用本机 RimWorld、RJW、动画框架和 Harmony 依赖完成 Release 编译。
- 新增 [SharedBed 回归](../../Tests/SharedBed/README.md)，真实 Harmony 2.4.2 + 最小生命周期模型：76/76 通过，新增二十二项覆盖双向床伴、先分配有效床伴、空床身份限制、动态标签、既有床优先及三人睡眠。
- 另用游戏实际使用的 Harmony 2.4.1 和本机 `Assembly-CSharp.dll`，在独立 .NET Framework 进程验证 9 个补丁类、10 个实际方法的安装成功，包括分配与界面 transpiler；没有执行游戏方法，也没有启动游戏。
- 提供简中、繁中、英文、俄文的分配标记提示，以及新增调教员记忆翻译。

维护者已确认初版红字修复、Mint 兼容及本轮规则调整有效，并在空床分配修复后反馈“没有问题了”，授权更新文档并提交。具体字体/缩放、真实存档中途保存读取和跨模组组合的逐项覆盖未单独记录；测试模型的序列化检查不能代替真实 Scribe 存档往返。

本轮 DLL 与四语标签提示已备份并同步本机游戏目录，逐文件校验哈希一致。关系调整前备份为 `.builds/shared-bed-audit/before-bed-relations-20260916-155801`，空床分配修复前备份为 `.builds/shared-bed-audit/before-empty-bed-fix-20260916-162655`；心情 Def XML 与工作区保持一致，本轮未修改它。完全重启游戏后再验证新标签、分配顺序和睡后记忆。

没有修改原版关系、已有床位归属或旧存档文件。失去额外许可后，后续床位使用重新由原版及当前有效许可检查；不主动批量迁移床位。

## 空殖民者床分配修复（2026-09-16）

移除“空床允许性奴先分配”的身份例外：即使恶堕达标，原版奴隶身份的性奴也不能直接分配空殖民者床。操作顺序为先给性奴指定调教员，再将该调教员或绑定主人分配到普通多人床，最后加入性奴。殖民者身份的性奴继续按原版规则分配；已有归属不被强制解除。

回归覆盖空床拒绝、主人或调教员先分配后允许加入、加入前移除床伴后恢复拒绝，以及殖民者身份对照。四语悬停提示同步说明分配顺序。该修复在实现阶段随关系与标签调整一并提交，当时未单独提升版本号；现已纳入 2.3.0 正式发布。

## 起床红字修复（2026-09-16）

本机安装目录使用新版 DLL，但 `Thought_SharedBed.xml` 仍是旧版：主人心情引用已经删除的情境心情 worker，调教员定义完全缺失。启动日志同时出现旧类无法解析和 `SSC_SharedBedWithTrainer` 缺失；起床日志先提示主人定义不是记忆，再在删除调教员记忆时因空定义报错（`211EF786`）。手动添加后的原始堆栈（`7DAC62ED`）位于 `Thought_Situational.CurrentStateInternal`，同样与残留的旧情境定义相符。

原版 `RemoveMemoriesOfDef` 会直接读取 `def.IsMemory`。结算现已先检查目标定义为记忆，再仅清理非空且属于记忆的定义；文件不配套时保留已有记忆，并只记录一次明确的更新提示。原测试模型的列表删除错误地容忍空定义，已按原版行为修正，新增四项缺失/旧版定义回归。

已备份安装目录原文件至 `.builds/shared-bed-audit/installed-backup-20260916-122226`，并同步、逐个校验 DLL、心情 XML、四语界面提示和三语调教员翻译，共九个文件。必须完全退出再启动 RimWorld，进程内已经加载的旧 Def 不会因覆盖磁盘文件自动更新。游戏内复测由维护者完成，后续反馈目前没有问题，同意提交。

## Dubs Mint Menus 分配界面兼容（2026-09-16）

Mint 使用自己的 `DubsMintMenus.Dialog_AssignBuildingOwner.DoRow(Rect, Pawn, bool)`，不会经过原版行绘制方法。新增可选反射补丁，为该方法中已分配/未分配角色的姓名绘制接入相同的性奴标记与提示；预留原有右侧 170px 按钮和 5px 间距。普通奴隶、其他建筑、分配逻辑、拒绝原因和独立的意识形态说明保持原有路径。

按本机 Mint 1.6 程序集验证两处姓名绘制替换；没有增加编译引用或必需依赖。独立进程分别验证加载 Mint 时成功安装一个兼容补丁，未加载时跳过。三项新增回归覆盖界面调用和区域。本次已更新 DLL，沿用已同步的心情定义和翻译，并补齐相关函数注释；维护者反馈没有问题，同意提交。

## 独立标签与同床状态（2026-09-16）

- 每个身份单独绘制 20px 高的色块，仅包含身份名称。主人通常同时显示“主人”“调教员”，启用调教员身份的性奴同时显示“性奴”“调教员”。
- 标签组右对齐，占姓名区域最多 55%；姓名起点不变，长文本省略，悬停姓名可读完整姓名及原版拒绝原因。Mint 继续为右侧按钮与意识形态说明保留 170px 加 5px 间距。
- 标签组旁的 7px 状态标记：绿色表示当前床上有符合 SSC 许可的对象；黄色表示空床等待有效床伴，或符合现有归属保留例外；灰色表示当前无额外许可。两种黄色状态由提示区分。角色身份色保持不变。
- 提示采用“主人：Daniel”“指定调教员：Riley”的字段格式，即使为同一人也分别显示；停用的指定调教员保留名字并注明不可用。
- 性奴显示当前恶堕百分比、是否达标及严格 `>0.1%` 的要求；主人/调教员查看相关已分配性奴的门槛，不用其自身恶堕代替。兼任性奴者分别展示本人及相关性奴数据。
- SSC 同床状态直接复用生产许可，列出实际达标床伴。空床不会仅因恶堕达标显示绿色；已有床位保留也不会被解释为存在合法床伴。状态不代替原版分配按钮、拒绝原因或环境检查，不新增硬限制。
- 简中、繁中、英文、俄文同步，修改函数附中文注释。Release 编译通过；SharedBed 93/93 通过（新增 11 项身份颜色、姓名起点、四语布局、门槛、双向许可及归属提示用例）；真实游戏程序集与 Mint 已安装/未安装两种补丁检查通过。
- 自动测试验证绘制调用与矩形、状态恢复及补丁 IL；维护者随后确认实机测试有效，授权更新文档并提交；实现阶段未单独发布，后统一纳入 2.3.0。

本轮 DLL 与四语提示已备份并同步本机游戏目录，五个文件均已校验哈希。备份：`.builds/shared-bed-audit/before-independent-badges-20260916-184508`。维护者完成本轮游戏内测试并确认有效。
